{
  "name": "web-rtc-example",
  "version": "1.0.0",
  "scripts": {
    "install": "run-p client:install backend:install",
    "start": "run-p client:start backend:start",
    "client:install": "cd frontend && yarn",
    "backend:install": "cd backend && yarn",
    "client:start": "cd frontend && yarn dev",
    "backend:start": "cd backend && yarn dev"
  },
  "author": "propertiess",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
